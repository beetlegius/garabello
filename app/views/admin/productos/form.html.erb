<%= content_for :header do %>
  <!-- Content Header (Page header) -->
  <section class="content-header">
    <h1>
      <%= Producto.model_name.human %>
      <small><%= @producto&.to_label %></small>
    </h1>
    <ol class="breadcrumb">
      <li><%= link_to fa_icon(:wrench, text: Producto.model_name.human), admin_productos_path %></li>
      <li class="active"><%= t(action_name, scope: :actions) %></li>
    </ol>
  </section>
<% end %>

<div class="row">
  <div class="col-xs-12">
    <div class="box box-primary">
      <div class="box-header">
        <h3 class="box-title"><%= @producto.to_label || 'Nuevo producto' %></h3>
      </div>

      <%= simple_form_for [:admin, @producto], html: { role: :form } do |f| %>
        <div class="box-body">
          <div class="row">
            <div class="col-xs-12 col-sm-4">
              <%= f.input :nombre, disabled: true %>
            </div>
            <div class="col-xs-12 col-sm-4">
              <%= f.input :codigo, disabled: true %>
            </div>
            <div class="col-xs-12 col-sm-4">
              <%= f.input :nombre_planilla, autofocus: true, input_html: { placeholder: true } %>
            </div>
          </div>
        </div><!-- /.box-body -->

        <div class="box-footer">
          <%= f.button :submit, class: 'btn btn-primary', data: { disable_with: t(:wait) } %>
        </div>
      <% end %>

    </div>
  </div>
</div>
