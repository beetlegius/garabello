var datatable = $("table[role='grid']").DataTable();
$("[data-behavior='download']").attr("href", "<%= admin_programas_path(params.permit!.merge(format: :csv)) %>");
datatable.ajax.url("<%= admin_programas_path(params.permit!) %>");
datatable.ajax.reload();
